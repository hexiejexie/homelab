x-traefik-labels: &traefik-template
  traefik.enable: true
  # HTTP → HTTPS redirect
  traefik.http.routers.delivery-cosmin-http.rule: Host(`delivery-cosmin.pulosu.hexie.dev`) || Host(`delivery.hexie.dev`)
  traefik.http.routers.delivery-cosmin-http.entrypoints: web
  traefik.http.routers.delivery-cosmin-http.middlewares: redirect-to-https
  traefik.http.middlewares.redirect-to-https.redirectscheme.scheme: https
  # HTTPS router
  traefik.http.routers.delivery-cosmin-https.rule: Host(`delivery-cosmin.pulosu.hexie.dev`) || Host(`delivery.hexie.dev`)
  traefik.http.routers.delivery-cosmin-https.entrypoints: websecure
  traefik.http.routers.delivery-cosmin-https.tls: true
  traefik.http.routers.delivery-cosmin-https.tls.certresolver: cloudflare
  # Match router to a named service
  traefik.http.routers.delivery-cosmin-https.service: delivery-cosmin
  # Define internal port for the service
  traefik.http.services.delivery-cosmin.loadbalancer.server.port: ${APP_PORT}

x-homepage-labels: &homepage-template
  homepage.group: ${HOMEPAGE_GROUP}
  homepage.name: ${HOMEPAGE_NAME}
  homepage.icon: ${HOMEPAGE_ICON}
  homepage.href: https://${APP_NAME}.${BASE_DOMAIN}
  homepage.description: ${HOMEPAGE_DESCRIPTION}
  #homepage.ping: https://${APP_NAME}.${BASE_DOMAIN}



services:
  delivery-cosmin:
    container_name: ${APP_NAME}
    image: hexiejexie/delivery-app:latest
    restart: unless-stopped
    volumes:
      - ./romania:/app/romania          # Word templates
      - ./doc_number.txt:/app/doc_number.txt  # persistent document counter
    networks:
      - frontend
    labels:
      <<: [*traefik-template, *homepage-template]

networks:
  frontend:
    external: true