services:
  terraria:
    container_name: ${APP_NAME}
    image: passivelemon/terraria-docker:${IMAGE_TAG:-tmodloader-latest}
    restart: unless-stopped
    environment:
      PUID: "1000"
      PGID: "1000"
    ports:
      - 7777:7777
    volumes:
      - ./config:/opt/terraria/config