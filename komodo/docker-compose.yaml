x-traefik-labels: &traefik-template
  traefik.enable: true
  traefik.http.routers.komodo-http.rule: Host(`${APP_NAME}.${BASE_DOMAIN}`)
  traefik.http.routers.komodo-http.entrypoints: web
  traefik.http.routers.komodo-http.middlewares: redirect-to-https
  traefik.http.middlewares.redirect-to-https.redirectscheme.scheme: https
  traefik.http.routers.komodo-https.rule: Host(`${APP_NAME}.${BASE_DOMAIN}`)
  traefik.http.routers.komodo-https.entrypoints: websecure
  traefik.http.routers.komodo-https.tls: true
  traefik.http.routers.komodo-https.tls.certresolver: cloudflare
  traefik.http.routers.komodo-https.service: ${APP_NAME}
  traefik.http.services.komodo.loadbalancer.server.port: ${APP_PORT}

x-homepage-labels: &homepage-template
  homepage.group: ${HOMEPAGE_GROUP}
  homepage.name: ${HOMEPAGE_NAME}
  homepage.icon: ${HOMEPAGE_ICON}
  homepage.href: https://${APP_NAME}.${BASE_DOMAIN}
  homepage.description: ${HOMEPAGE_DESCRIPTION}
  homepage.widget.type: komodo
  homepage.widget.url: http://${APP_NAME}:${APP_PORT}
  homepage.widget.key: ${HOMEPAGE_WIDGET_KEY}
  homepage.widget.secret: ${HOMEPAGE_WIDGET_SECRET}
  homepage.widget.showSummary: true
  homepage.widget.showStacks: true

x-kuma-labels: &kuma-template
  kuma.komodo.http.name: ${HOMEPAGE_NAME}
  kuma.komodo.http.url: https://${APP_NAME}.${BASE_DOMAIN}
  kuma.komodo.http.max_retries: 3
  kuma.komodo.http.interval: 60

services:
  mongo:
    container_name: mongo-komodo
    image: mongo:${MONGO_TAG:-8}
    restart: unless-stopped
    command: --quiet --wiredTigerCacheSizeGB 0.25
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${KOMODO_DB_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${KOMODO_DB_PASSWORD}
    volumes:
      - ./data/mongo-data:/data/db
      - ./data/mongo-config:/data/configdb
    networks:
      - backend
    labels:
      komodo.skip: true

  core:
    container_name: ${APP_NAME}
    image: ghcr.io/moghtech/komodo-core:${COMPOSE_KOMODO_IMAGE_TAG:-latest}
    restart: unless-stopped
    depends_on:
      - mongo
    environment:
      KOMODO_DATABASE_ADDRESS: mongo:27017
      KOMODO_DATABASE_USERNAME: ${KOMODO_DB_USERNAME}
      KOMODO_DATABASE_PASSWORD: ${KOMODO_DB_PASSWORD}
    volumes:
      - ${COMPOSE_KOMODO_BACKUPS_PATH}:/backups
      - ./config/core.config.toml:/config/config.toml
    networks:
      - frontend
      - backend
    labels:
      <<: [*traefik-template, *homepage-template, *kuma-template]

  periphery:
    container_name: komodo-periphery
    image: ghcr.io/moghtech/komodo-periphery:${COMPOSE_KOMODO_IMAGE_TAG:-latest}
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /proc:/proc
      - ${PERIPHERY_ROOT_DIRECTORY:-/etc/komodo}:${PERIPHERY_ROOT_DIRECTORY:-/etc/komodo}
    networks:
      - backend
    labels:
      komodo.skip: true

networks:
  frontend:
    external: true
  backend:
    external: true