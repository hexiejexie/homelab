x-traefik-labels: &traefik-template
  traefik.enable: true
  # HTTP → HTTPS redirect
  traefik.http.routers.myspeed-http.rule: Host(`${APP_NAME}.${BASE_DOMAIN}`)
  traefik.http.routers.myspeed-http.entrypoints: web
  traefik.http.routers.myspeed-http.middlewares: redirect-to-https
  traefik.http.middlewares.redirect-to-https.redirectscheme.scheme: https
  # HTTPS router
  traefik.http.routers.myspeed-https.rule: Host(`${APP_NAME}.${BASE_DOMAIN}`)
  traefik.http.routers.myspeed-https.entrypoints: websecure
  traefik.http.routers.myspeed-https.tls: true
  traefik.http.routers.myspeed-https.tls.certresolver: cloudflare
  # Match router to a named service
  traefik.http.routers.myspeed-https.service: myspeed
  # Define internal port for the service
  traefik.http.services.myspeed.loadbalancer.server.port: ${APP_PORT}

x-homepage-labels: &homepage-template
  homepage.group: ${HOMEPAGE_GROUP}
  homepage.name: ${HOMEPAGE_NAME}
  homepage.icon: ${HOMEPAGE_ICON}
  homepage.href: https://${APP_NAME}.${BASE_DOMAIN}
  homepage.description: ${HOMEPAGE_DESCRIPTION}
  #homepage.ping: https://${APP_NAME}.${BASE_DOMAIN}



services:
  myspeed:
    container_name: ${APP_NAME}
    image: germannewsmaker/myspeed:latest
    restart: unless-stopped
    volumes:
      - ./config:/myspeed/data
    networks:
      - frontend
    labels:
      <<: [*traefik-template, *homepage-template]

networks:
  frontend:
    external: true